[project]
name = "ai-native-starter"
version = "0.1.0"
description = "Opinionated starter for responsibility-oriented AI applications"
requires-python = ">=3.11"

[project.scripts]
ai-native = "ainative.cli:main"

[tool.pyright]
include = ["ainative"]

